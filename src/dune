
(library
  (name parany)
  (public_name parany)
  (modules parany)
  (libraries unix threads netmulticore netsys cpu))

(executables
  (names test send)
  (modules test send)
  (libraries parany unix sendmsg bytearray))

(alias
  (name runtest)
  (action
  (run ./test.exe 16 800)))
