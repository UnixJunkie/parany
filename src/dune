
(library
  (name parany)
  (public_name parany)
  (modules parany)
  (libraries unix threads netmulticore netsys cpu))

(executable
  (name test)
  (modules test)
  (libraries parany unix))

(alias
  (name runtest)
  (action
  (run ./test.exe 16 800)))
