
(library
  (name parany)
  (public_name parany)
  (modules parany)
  (libraries unix cpu sendmsg))

(executables
  (names test)
  (modules test)
  (libraries parany unix bytearray))

(alias
  (name runtest)
  (action
  (run ./test.exe 16 800)))
