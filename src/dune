
(library
  (name parany)
  (public_name parany)
  (modules parany)
  (libraries unix threads netmulticore netsys cpu))

(executables
  (names test send)
  (modules test send utls)
  (libraries parany unix sendmsg))

(alias
  (name runtest)
  (action
  (run ./test.exe 16 800)))
